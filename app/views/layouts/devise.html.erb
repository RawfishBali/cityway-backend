<!DOCTYPE html>
<html>
<head>
  <title>CityWay</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.3/css/font-awesome.min.css">
  <link href="https://fonts.googleapis.com/css?family=Poppins" rel="stylesheet">
  <%= stylesheet_link_tag 'login_admin', media: 'all', 'data-turbolinks-track' => true %>



  <%= csrf_meta_tags %>
</head>
<body class="login_page">
  <% flash.each do |type, message| %>
    <% unless message == 'true' %>
      <div class="uk-notify uk-notify-top-center" style="display: block;">
        <div class="uk-notify-message" style="opacity: 1; margin-top: -50px; margin-bottom: 10px;">
          <a class="uk-close"></a>
          <div>
            <%= message %>
          </div>

        </div>
      </div>
    <% end %>%>
  <% end %>




  <%= yield %>
  <%= javascript_include_tag 'admin'%>

</body>
</html>
