<div class="md-card uk-margin-medium-bottom">
    <div class="md-card-content">
        <div class="uk-overflow-container">
            <table class="uk-table uk-table-nowrap table_check">
                <thead>
                <tr>
                    <th class="uk-width-1-10">Name</th>
                    <th class="uk-width-1-10 uk-text-center">Url</th>
                    <th class="uk-width-1-10 uk-text-center">Actions</th>
                </tr>
                </thead>
                <tbody>
                    <% @admin_online_services.each do  |service|%>
                    <tr>
                        <td class=""><%= service.name  %></td>
                        <td class="uk-text-center"><%= service.url %></td>
                        <td class="uk-text-center">
                            <%= link_to '<i class="md-icon material-icons">&#xE254;</i>'.html_safe , edit_admin_online_service_path(service)%>
                            <%= link_to '<i class="md-icon material-icons ">&#xE872;</i>'.html_safe, admin_online_service_path(service), method: :delete , :data => {:confirm => 'Sei sicuro?'} %>
                        </td>
                    </tr>
                    <% end %>
                </tbody>
            </table>
            <%= paginate @admin_online_services %>
        </div>
    </div>
    <div class="md-fab-wrapper">
        <a class="md-fab md-fab-accent md-fab-wave-light waves-effect waves-button waves-light" href="/admin/online_services/new" id="invoice_add">
            <i class="material-icons"></i>
        </a>
    </div>
</div>
