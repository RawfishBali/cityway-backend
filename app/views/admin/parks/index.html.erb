<div class="md-card uk-margin-medium-bottom">
    <div class="md-card-content">
        <div class="uk-overflow-container">
            <table class="uk-table uk-table-nowrap table_check">
                <thead>
                <tr>
                    <th class="uk-width-1-10">Nome</th>
                    <th class="uk-width-1-10 uk-text-center">Indirizzo</th>
                    <th class="uk-width-1-10 uk-text-center">Descrizione</th>
                    <th class="uk-width-1-10 uk-text-center">Foto</th>
                    <th class="uk-width-1-10 uk-text-center">Azioni</th>
                </tr>
                </thead>
                <tbody>
                    <% @admin_parks.each do  |park|%>
                    <tr>

                        <td class=""><%= park.name  %></td>
                        <td class="uk-text-center"><%= park.address %></td>
                        <td class="uk-text-center"><%= truncate(park.description, length: 25, omission: '...') %></td>
                        <td class="uk-text-center"><%= image_tag park.primary_photo.first.picture.url, class: 'width100pixel' unless park.primary_photo.first.blank?  %></td>

                        <td class="uk-text-center">
                            <%= link_to '<i class="md-icon material-icons">&#xE254;</i>'.html_safe , edit_admin_park_path(park)%>
                            <%= link_to '<i class="md-icon material-icons ">&#xE872;</i>'.html_safe, admin_park_path(park), method: :delete , :data => {:confirm => 'Sei sicuro di voler cancellare?'} %>

                        </td>
                    </tr>
                    <% end %>

                </tbody>
            </table>
            <%= paginate @admin_parks %>
        </div>
    </div>
    <div class="md-fab-wrapper">
        <a class="md-fab md-fab-accent md-fab-wave-light waves-effect waves-button waves-light" href="/admin/parks/new" id="invoice_add">
            <i class="material-icons"></i>
        </a>
    </div>
</div>
