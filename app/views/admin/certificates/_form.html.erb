<div class="md-card">
  <div class="md-card-content">
    <%= form_for(@admin_certificate, url: (@admin_certificate.new_record? ? admin_certificates_path(@admin_certificate) : admin_certificate_path(@admin_certificate)), multipart: true) do |f| %>
    <% if @admin_certificate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_certificate.errors.count, "error") %> prohibited this admin_category from being saved:</h2>

      <ul>
        <% @admin_certificate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>
    <h2 class=" ">
      <% if @admin_certificate.new_record?%>
      New <%= @admin_certificate.class.name %>
      <% else %>
      Edit <%= @admin_certificate.name %>
      <% end %>
      <%= link_to '<i class="fa fa-arrow-circle-left fa-2x" aria-hidden="true"></i>'.html_safe, :back, class:'float-right' %>
    </h2>
    <div class="uk-grid" data-uk-grid-margin>
      <div class="uk-width-medium-1-2">
        <div class="uk-form-row">
          <div class="uk-grid" data-uk-grid-margin>
            <div class="uk-width-medium-1-1">
              <label>Nome</label>
              <%= f.text_field :name,class: 'md-input', required: true  %>
            </div>
          </div>
        </div>
        <div class="uk-form-row">
          <div class="uk-grid" data-uk-grid-margin>
            <div class="uk-width-medium-1-1">
              <label>Url</label>
              <%= f.text_field :url,class: 'md-input', required: true  %>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="md-fab-wrapper">
      <%= button_tag(type: 'submit' , class: 'md-fab md-fab-accent md-fab-wave waves-effect waves-button') do %>
      <i class="fa fa-floppy-o" aria-hidden="true"></i>
      <% end %>
    </div>
    <% end %>
  </div>
